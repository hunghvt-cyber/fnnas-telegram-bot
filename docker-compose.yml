version: "3.9"

services:
  telegrambot:
    container_name: telegrambot

    build: .

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

    working_dir: /app